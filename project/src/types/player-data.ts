export type PlayerData ={
  currentScore: number,
  totalScore: number,
};

export type Player1Data ={
  currentScore: number,
  totalScore: number,
};

export type Player2Data ={
  currentScore: number,
  totalScore: number,
};
